#server:
#  port: 9010
spring:
  application:
    name: msv-customer
    version: "@project.version@"
  profiles:
    active: prod
  cloud:
    config:
      uri: ${CONFIG_SERVER:http://localhost:8888}
  output:
    ansi:
      enabled: ALWAYS